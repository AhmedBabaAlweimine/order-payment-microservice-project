#-----------------PROD CONFIG -------------------------------##########

eureka:
  instance:
    prefer-ip-address: false           # ✅ ensures hostname is used, not pod IP
    hostname: gateway-ms-app-service   # ✅ use your  payment K8s service name in deployement as hostname